package c2.inheritance.interfacee;

// class, enum, interface --> type
public interface Shape {
	// abstract methods --> hàm trừu tượng
	// bắt buộc override trong KDL(ko phải trừu tượng) con
	
	// mặc định: public abstract
	
	void paint();
	
	void calS();
}
