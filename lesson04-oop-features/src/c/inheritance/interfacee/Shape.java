package c.inheritance.interfacee;

// class, enum, interface --> 'type'
public interface Shape {

	// abstract methods --> hàm trừu tượng
	// bắt buộc phải được override trong KDL(ko phải kiểu trừu tượng)
	
	void paint();
	
	void calS();
}
