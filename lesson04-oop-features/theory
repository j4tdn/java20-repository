				CÁC TÍNH CHẤT CỦA OOP: 

1. encapsulation: Đóng gói
 * Tính chất đóng gọi được thể hiện qua các access modifier: 
 	+Private: đóng gói trong phạm vi của Class
 	+_| protected(thừa kế): đóng gói trong phạm vi của package
 		protected: 
 	+public: được sử dụng ở bất kì đâu trong dự án 
 	
 2. polymorphism: Đa hình
 * Tính chất đa hình có 2 loại: 
 	+ đa hình trong phương thức 
 	+ đa hình trong đối tượng 
 	phương thức, đối tượng được gọi là đa hình nếu cùng 1 đối tượng, phương thức
 	nhưng lúc thì có thể hiền này lúc thì thể hiện khác --> Đa Hình 
 	
 	2.1 Đa hình trong phương thức
 		+ Overloading:
 			hai/ nhiều phương thức được gọi là overloading nếu:
 				+ thuộc cùng class
 				+ cùng tên 
 				+ khác số lượng tham số 
 				+ khác KDL truyền vào của tham số 
 				VD: sum(int, int), sum(int, int, int) 
 				  : float sum(float, float) 	
 		+Overriding:
 			hai/ hay nhiều  phương thức được gọi là overriding: 
 			+ Cùng phần khai báo hàm: void log()
 			+ thuộc hai hay nhiều class có quan hệ cha con 
 			+ Lớp con định nghĩa/ override lại phần thân hàm từ hàm ở lớp Cha
 				VD: toString, equals, log
	2.2: Đa hình trong đối tượng
		+ Lấy, gán giá trị của KDL cha/con cho KDL cha
		+ Nói cách khác: Lấy biến kiểu cha = kiểu cha/ con
		+ Mục đích: 
				+ giúp chuyển đổi qua về giữa các KDL Cha con
				+ Tạo một mảng/ danh sách các phần tử kiểu cha con
				+ viết ra phương thức chung có tham số là KDL cha
					để nhận vào giá trị thuộc KDL Cha/ con
		VD: callShapeMethods(Shape shape)  
		
		
3. Inheritance: Tính chất thừa kế
	3.1 Định nghĩa: khi hai hoặc nhiều đối tượng có chung thuốc tính, phương thức thay vì ở mỗi class
	phải tạo đi tạo lại các thuộc tính phương thức đó (trùng lặp code, ko tổng quát hóa) thì mình sẽ
	tạo ra Kiểu cha để chứa các thuộc tính, phương thưc chung đó
	
	VD: Sách giáo khoa: mã sách, tên sách, nhà xuất bản, trạng thái(cũ, mới), giảm giá;
		Sách tham khảo: mã sách, tên sách, nhà xuất bản, thuế(%);
		
 	3.2 KDL Cha/con: Interface, abstract class(Lop Truu Tuong)
+ KDL Cha: Class;
+ KDL Con: Class;
--> Khi có KDL con chung thuộc tính đưa những thuộc tính chung đó vào Class Cha 
--> rồi lấy class con kế thừa extends, reuse

+ KDL Cha: interface;
+ KDL Con: Class;
--> khi mà KDL con có chung phương thức 
	--> đưa phương thức 'chung'(biết phần khai báo (tên hàm và KDL trả về)) 
	--> đưa phương thức chung vào KDL cha nhưng chưa biết phần thực thi
	--> hàm chung trong KDL Cha nên là không có body --> KDL interface
	--> KDL con khi mà kế thừa từ KDL Cha phải override lại hàm chung đó

+ KDL Cha: Abstract Class
+ KDL con: Class
	--> khi mà KDL Cha vừa có hàm trừu tượng vừa có hàm bình thường 
	--> hàm bình thường --> body method
	--> chứa các thuộc tính chung của KDL con

what is the difference between interface & abstract class
		-answer
		+Difference:
			+ From before JDK 1.8: 
				+interface:
					.only have abstract methods
					.support multiple implements
					.attribute --> auto 'public static final'
					.method --> auto public abstract
				+AbstractClass
					.can have abstract & body methods
					.support single extends
					.attribute --> nothing special, belong to object scope
					.method --> depends on access modifier
					
			+From after JDK 1.8:
				+interface: 
					.can have abstract & body methods(default, static, private)
				+abstracClass:
					.can have abstract & body methods

