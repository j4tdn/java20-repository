1. Class là gì  Object là gì 
	phân biệt
	
2. Tại sao phải khai báo phạm vi truy cập   
    là private cho các thuộc tính của class
	 
	
	rồi sau đó dùng hàm getter, setter để truy cập thuộc tính ? 
	
3. Phân biệt constructor(khởi tạo đối tượng mới) và setter

4. khi nào sẽ khai báo static cho thuộc tính, hàm ?

5. có thể khai báo private constructor được không vì sao? vì sao cho ví dụ 

	+ với 1 class bình thường --> dùng public constructor để khởi tạo đối tượng 
	+cũng có trường hợp dùng private constructor
		--> khi ko muốn class đó cho phép tạo đối tượng nào ra cả
	
	vd: Utility class --> Util Class --> class chứa những util methods
		MathUtil: 
		private MathUtil(){}
				+static sum(a, b);
				+static sub(a, b);
				+static abs(a);
				+static mul(a,b);
